"""Free news source registry.

Each source is described once here so the rest of the pipeline doesn't need
to know how it's fetched. Week 1 will fill in the actual feed URLs and the
``fetch()`` implementations for each source type.
"""
